import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pelupet.app',   // Cambia por el identificador que uses
  appName: 'PeluPet',
  webDir: 'www'

};


export default config;


